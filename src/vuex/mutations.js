export default {
  getHit(state, object) {
    state.hit = object
  },
  getSoon(state, object) {
    state.soon = object
  },
  getRanking(state, object) {
    state.ranking = object
  }
  // itemdata(state, obj) {
  //   state.itemd = obj
  // }
}
